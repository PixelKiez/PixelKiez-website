{
  "name": "bds-website",
  "version": "1.0.0",
  "description": "Berlin Digital Systems — Website. Quelle in site/, Auslieferung aus dist/.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "verify": "node scripts/verify.mjs",
    "serve": "node scripts/serve.mjs",
    "clean": "rm -rf dist",
    "i18n": "node scripts/i18n-extract.mjs",
    "deps": "sh scripts/deps.sh"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "html-minifier-terser": "^7.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
